# AWS App Runner — сборка и запуск бекенда
# Документация: https://docs.aws.amazon.com/apprunner/latest/dg/service-source-code-nodejs.html

version: 1.0
runtime: nodejs22

build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build

run:
  command: node dist/server.js
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
    - name: NODE_ENV
      value: "production"
    - name: JWT_SECRET
      value: "your-super-secret-jwt-key-change-this-in-production-snapverse-2024"
    - name: FRONTEND_URL
      value: "https://ugs-portfolio.vercel.app,https://www.ugs-info.at,https://ugs-info.at"
