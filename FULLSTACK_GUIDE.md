# 🚀 SnapVerse Full-Stack Guide

## 📋 Что создано

### ✅ Backend (Node.js + Express + MongoDB + JWT)

**Папка:** `F:\UGS\backend`

**Технологии:**
- Node.js + Express
- TypeScript
- MongoDB + Mongoose
- JWT Authentication
- bcrypt (хеширование паролей)
- CORS

**Функционал:**
- ✅ Регистрация пользователей (signup)
- ✅ Вход (login)
- ✅ JWT токены
- ✅ Создание, редактирование, удаление постов
- ✅ Лайки и комментарии
- ✅ Поиск пользователей
- ✅ Управление профилем
- ✅ Follow/Unfollow

### ✅ Frontend (React + TypeScript + Tailwind CSS)

**Папка:** `F:\UGS\src`

**Технологии:**
- React 18
- TypeScript
- Tailwind CSS
- React Router
- i18next (мультиязычность)

**Функционал:**
- ✅ Красивые формы регистрации/входа
- ✅ Подключение к реальному backend API
- ✅ Сохранение JWT токенов
- ✅ Модальные окна успеха/ошибок

---

## 🎯 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                        ПОЛЬЗОВАТЕЛЬ                         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  FRONTEND (React + Vite)                    │
│                  http://localhost:5175                      │
│                                                             │
│  • Формы регистрации/входа                                 │
│  • API Client (src/api/client.ts)                          │
│  • JWT токены в localStorage                               │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP Requests
                         │ Authorization: Bearer TOKEN
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              BACKEND API (Node.js + Express)                │
│                  http://localhost:5000                      │
│                                                             │
│  • JWT Authentication Middleware                           │
│  • Routes: /api/auth, /api/posts, /api/users              │
│  • Controllers: Business Logic                             │
│  • Models: Mongoose Schemas                                │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Mongoose ODM
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  DATABASE (MongoDB)                         │
│              mongodb://localhost:27017/snapverse            │
│                                                             │
│  Collections:                                              │
│  • users   - Пользователи (с хешированными паролями)      │
│  • posts   - Посты с лайками и комментариями              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 Запуск проекта

### Шаг 1: Установить MongoDB

**Вариант А: Локально (Windows)**
```powershell
# Скачать и установить MongoDB Community
# https://www.mongodb.com/try/download/community

# Проверить что запущен
Get-Service MongoDB
```

**Вариант Б: MongoDB Atlas (облако, бесплатно)**
- Зарегистрироваться на https://www.mongodb.com/cloud/atlas
- Создать бесплатный кластер (M0)
- Получить строку подключения
- Обновить `backend/.env`:
  ```
  MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/snapverse
  ```

### Шаг 2: Запустить Backend

```powershell
# Терминал 1: Backend
cd F:\UGS\backend
npm install
npm run dev
```

**Ожидаемый вывод:**
```
✅ MongoDB connected successfully
📊 Database: snapverse

🚀 SnapVerse Backend Server Started
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📡 Server running on port 5000
🌐 API URL: http://localhost:5000/api
```

### Шаг 3: Запустить Frontend

```powershell
# Терминал 2: Frontend
cd F:\UGS
npm run dev
```

**Ожидаемый вывод:**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5175/
  ➜  Network: use --host to expose
```

### Шаг 4: Открыть в браузере

```
http://localhost:5175/about
```

---

## 🧪 Тестирование

### 1. Проверить Backend

```powershell
# Health check
curl http://localhost:5000/api/health
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "SnapVerse API is running",
  "timestamp": "2024-11-14T..."
}
```

### 2. Зарегистрировать пользователя

**В браузере:**
1. Открыть http://localhost:5175/about
2. Заполнить форму регистрации:
   - Email: `test@example.com`
   - Full Name: `Test User`
   - Username: `testuser`
   - Password: `password123`
3. Нажать "Sign up"
4. Должно появиться модальное окно "Success!"

**Через Postman/curl:**
```powershell
curl -X POST http://localhost:5000/api/auth/signup `
  -H "Content-Type: application/json" `
  -d '{\"email\":\"test@example.com\",\"fullName\":\"Test User\",\"username\":\"testuser\",\"password\":\"password123\"}'
```

### 3. Войти

1. На странице About нажать "Log in"
2. Ввести:
   - Username: `testuser`
   - Password: `password123`
3. Нажать "Log in"
4. Должно появиться "Welcome back testuser!"

### 4. Проверить токен в localStorage

**В браузере (F12 → Console):**
```javascript
localStorage.getItem('auth_token')
// Должен вернуть JWT токен
```

---

## 📂 Структура проекта

```
F:\UGS\
├── backend/                    # Backend API
│   ├── src/
│   │   ├── config/
│   │   │   └── database.ts     # MongoDB подключение
│   │   ├── controllers/
│   │   │   ├── authController.ts
│   │   │   ├── postController.ts
│   │   │   └── userController.ts
│   │   ├── middleware/
│   │   │   └── auth.ts         # JWT middleware
│   │   ├── models/
│   │   │   ├── User.ts         # User schema
│   │   │   └── Post.ts         # Post schema
│   │   ├── routes/
│   │   │   ├── auth.ts
│   │   │   ├── posts.ts
│   │   │   └── users.ts
│   │   └── server.ts           # Main server
│   ├── .env                    # Environment variables
│   ├── package.json
│   ├── tsconfig.json
│   └── README.md
│
├── src/                        # Frontend
│   ├── api/
│   │   └── client.ts           # API client для backend
│   ├── pages/
│   │   └── About/
│   │       └── About.tsx       # Страница с формами
│   ├── data/
│   │   └── dbMassages.ts       # Старый mock API (для справки)
│   └── ...
│
├── .env.local                  # Frontend env (API URL)
├── BACKEND_API.md              # Документация mock API
├── SETUP_BACKEND.md            # Инструкция по установке
├── FULLSTACK_GUIDE.md          # Этот файл
└── HOW_TO_VIEW_DATA.md         # Как смотреть данные
```

---

## 🔐 Как работает аутентификация

### 1. Регистрация (Signup)

```
Frontend                Backend                 MongoDB
   │                       │                       │
   │  POST /api/auth/signup│                       │
   │──────────────────────>│                       │
   │  {email, username,    │                       │
   │   password}           │                       │
   │                       │                       │
   │                       │  1. Check if user     │
   │                       │     exists            │
   │                       │──────────────────────>│
   │                       │<──────────────────────│
   │                       │                       │
   │                       │  2. Hash password     │
   │                       │     (bcrypt)          │
   │                       │                       │
   │                       │  3. Create user       │
   │                       │──────────────────────>│
   │                       │<──────────────────────│
   │                       │                       │
   │                       │  4. Generate JWT      │
   │                       │     token             │
   │                       │                       │
   │  {success, token,     │                       │
   │   user}               │                       │
   │<──────────────────────│                       │
   │                       │                       │
   │  5. Save token to     │                       │
   │     localStorage      │                       │
   │                       │                       │
```

### 2. Вход (Login)

```
Frontend                Backend                 MongoDB
   │                       │                       │
   │  POST /api/auth/login │                       │
   │──────────────────────>│                       │
   │  {username, password} │                       │
   │                       │                       │
   │                       │  1. Find user         │
   │                       │──────────────────────>│
   │                       │<──────────────────────│
   │                       │                       │
   │                       │  2. Compare password  │
   │                       │     (bcrypt.compare)  │
   │                       │                       │
   │                       │  3. Generate JWT      │
   │                       │                       │
   │  {success, token,     │                       │
   │   user}               │                       │
   │<──────────────────────│                       │
   │                       │                       │
   │  4. Save token        │                       │
   │                       │                       │
```

### 3. Защищённые запросы

```
Frontend                Backend                 MongoDB
   │                       │                       │
   │  POST /api/posts      │                       │
   │  Authorization:       │                       │
   │  Bearer TOKEN         │                       │
   │──────────────────────>│                       │
   │                       │                       │
   │                       │  1. Verify JWT        │
   │                       │     (middleware)      │
   │                       │                       │
   │                       │  2. Get user from     │
   │                       │     token             │
   │                       │──────────────────────>│
   │                       │<──────────────────────│
   │                       │                       │
   │                       │  3. Execute request   │
   │                       │──────────────────────>│
   │                       │<──────────────────────│
   │                       │                       │
   │  {success, data}      │                       │
   │<──────────────────────│                       │
```

---

## 📊 API Endpoints

### Authentication

| Method | Endpoint | Description | Auth |
|--------|----------|-------------|------|
| POST | `/api/auth/signup` | Регистрация | ❌ |
| POST | `/api/auth/login` | Вход | ❌ |
| GET | `/api/auth/me` | Текущий пользователь | ✅ |

### Posts

| Method | Endpoint | Description | Auth |
|--------|----------|-------------|------|
| GET | `/api/posts` | Все посты | ❌ |
| POST | `/api/posts` | Создать пост | ✅ |
| PUT | `/api/posts/:id` | Обновить пост | ✅ |
| DELETE | `/api/posts/:id` | Удалить пост | ✅ |
| POST | `/api/posts/:id/like` | Лайк | ✅ |
| DELETE | `/api/posts/:id/like` | Убрать лайк | ✅ |
| POST | `/api/posts/:id/comments` | Комментарий | ✅ |

### Users

| Method | Endpoint | Description | Auth |
|--------|----------|-------------|------|
| GET | `/api/users/search?q=query` | Поиск | ❌ |
| GET | `/api/users/:id` | Пользователь | ❌ |
| PUT | `/api/users/profile` | Обновить профиль | ✅ |
| POST | `/api/users/:id/follow` | Подписаться | ✅ |

---

## 🛠️ Разработка

### Добавить новый API endpoint

**1. Создать контроллер** (`backend/src/controllers/`)

```typescript
export const myNewEndpoint = async (req: AuthRequest, res: Response) => {
  // Your logic here
  res.json({ success: true, data: 'Hello!' });
};
```

**2. Добавить маршрут** (`backend/src/routes/`)

```typescript
router.get('/my-endpoint', protect, myNewEndpoint);
```

**3. Добавить в API client** (`src/api/client.ts`)

```typescript
export const myAPI = {
  callEndpoint: async () => {
    return await apiRequest('/my-endpoint');
  },
};
```

**4. Использовать в компоненте**

```typescript
import { myAPI } from '../../api/client';

const data = await myAPI.callEndpoint();
```

---

## 🐛 Решение проблем

### Backend не запускается

**Ошибка:** `MongoDB connection error`

**Решение:**
```powershell
# Проверить MongoDB
Get-Service MongoDB

# Запустить
Start-Service MongoDB
```

### Frontend не подключается к Backend

**Ошибка:** `CORS policy` или `Network Error`

**Решение:**
1. Проверить что backend запущен на порту 5000
2. Проверить `.env.local`:
   ```
   VITE_API_URL=http://localhost:5000/api
   ```
3. Перезапустить frontend:
   ```powershell
   npm run dev
   ```

### Токен не сохраняется

**Проблема:** После регистрации токен не появляется в localStorage

**Решение:**
1. Открыть DevTools (F12) → Console
2. Проверить ошибки
3. Убедиться что backend вернул токен:
   ```javascript
   // В консоли должно быть:
   ✅ Registration successful! {user: {...}}
   🔑 Token saved to localStorage
   ```

---

## 📝 Следующие шаги

### 1. Создать страницу профиля

- Показывать информацию пользователя
- Редактировать bio, avatar
- Показывать посты пользователя

### 2. Создать ленту постов

- Показывать все посты
- Лайкать/комментировать
- Создавать новые посты

### 3. Добавить загрузку изображений

- Использовать Cloudinary или AWS S3
- Загружать фото с компьютера

### 4. Деплой

**Backend:**
- Railway.app (бесплатно)
- Render.com (бесплатно)

**Frontend:**
- Vercel (бесплатно)
- Netlify (бесплатно)

**Database:**
- MongoDB Atlas (бесплатно до 512MB)

---

## 🎓 Что вы изучили

✅ **Backend разработка:**
- Node.js + Express
- REST API
- MongoDB + Mongoose
- JWT Authentication
- Bcrypt (хеширование паролей)
- TypeScript
- CORS

✅ **Frontend разработка:**
- React + TypeScript
- API интеграция
- Работа с токенами
- Async/await
- Error handling

✅ **Full-Stack:**
- Связь Frontend ↔ Backend
- Аутентификация
- Безопасность
- Архитектура приложения

---

## 📚 Полезные ссылки

- [Express.js Documentation](https://expressjs.com/)
- [MongoDB Documentation](https://docs.mongodb.com/)
- [Mongoose Documentation](https://mongoosejs.com/)
- [JWT.io](https://jwt.io/)
- [React Documentation](https://react.dev/)
- [TypeScript Documentation](https://www.typescriptlang.org/)

---

**🎉 Поздравляем! Вы создали полноценное Full-Stack приложение!**

**Автор:** Olena Yasynetska  
**Проект:** SnapVerse  
**Дата:** November 2025

